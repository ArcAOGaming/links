import type { AutonomousFinanceLinks } from '../types';
import { DOMAIN_AUTONOMOUS_FINANCE } from '../constants';

export const AUTONOMOUS_FINANCE_LINKS: AutonomousFinanceLinks = {
    website: `https://${DOMAIN_AUTONOMOUS_FINANCE}`
};
