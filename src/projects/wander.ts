import type { WanderLinks } from '../types';
import {
    DOMAIN_WANDER
} from '../constants';

export const ARCAO_LINKS: WanderLinks = {
    website: `https://${DOMAIN_WANDER}`,
    download: `https://${DOMAIN_WANDER}/download?tab=download-browser`
};